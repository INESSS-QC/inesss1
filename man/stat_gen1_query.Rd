% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SLQ_stat_gen1.R
\name{stat_gen1_query}
\alias{stat_gen1_query}
\title{Statistiques générales}
\usage{
stat_gen1_query(
  debut,
  fin,
  type_Rx = "DENOM",
  codes,
  result_by = NULL,
  code_serv = c("1", "AD"),
  code_serv_filtre = "Exclusion",
  code_list = NULL,
  code_list_filtre = "Inclusion"
)
}
\arguments{
\item{debut}{Date de début de la période d'étude au format \code{"AAAA-MM-JJ"}.}

\item{fin}{Date de fin de la période d'étude au format \code{"AAAA-MM-JJ"}.}

\item{type_Rx}{\code{"DENOM"} ou \code{"DIN"}. Indique le type de code analysé.}

\item{codes}{Vecteur comprenant le ou les codes d'analyse au format numérique, sans zéros.}

\item{result_by}{Afficher les résultats par :
\itemize{
\item \strong{\code{"Période"}} : Analyse de tous les \code{codes} pour une même période (somme des montants de tous les codes analysés).
\item \strong{\code{"Teneur"}} : Résultats par teneur du médicament (\code{SMED_COD_TENR_MED}).
\item \strong{\code{"Format"}} : Résultats par format d'acquisition du médicament.
}}

\item{code_serv}{Vecteur de type \code{character} comprenant le ou les codes de service à exclure ou à inclure, sinon inscrire \code{NULL}.}

\item{code_serv_filtre}{\code{"Inclusion"} ou \code{"Exclusion"} des codes de service \code{code_serv}, sinon inscrire \code{NULL}.}

\item{code_list}{Vecteur de type \code{character} comprenant le ou les codes de catégories de liste de médicaments à exclure ou à inclure, sinon inscrire \code{NULL}.}

\item{code_list_filtre}{\code{"Inclusion"} ou \code{"Exclusion"} des codes de catégories de liste de médicaments \code{code_list}, sinon inscrire \code{NULL}.}
}
\description{
Code de la requête SQL - chaîne de caractères.
}
\details{
\strong{debut, fin :} Doivent contenir le même nombre de valeurs.
}
\examples{
## Avantage d'utiliser cat() si c'est pour afficher dans la console :
# Avec cat()
cat(stat_gen1_query(
  debut = "2020-01-01", fin = "2020-12-31",
  type_Rx = "DENOM", codes = c(47092, 47135, 48222), result_by = NULL,
  code_serv = c("1", "AD"), code_serv_filtre = "Exclusion",
  code_list = NULL, code_list_filtre = "Inclusion"
))
# Sans cat()
stat_gen1_query(
  debut = "2020-01-01", fin = "2020-12-31",
  type_Rx = "DENOM", codes = c(47092, 47135, 48222), result_by = NULL,
  code_serv = c("1", "AD"), code_serv_filtre = "Exclusion",
  code_list = NULL, code_list_filtre = "Inclusion"
)

## RÃ©sultats par
# PÃ©riodes
cat(stat_gen1_query(
  debut = "2020-01-01", fin = "2020-12-31",
  type_Rx = "DENOM", codes = c(47092, 47135, 48222),
  result_by = "PÃ©riodes",
  code_serv = c("1", "AD"), code_serv_filtre = "Exclusion",
  code_list = NULL, code_list_filtre = "Inclusion"
))
# Teneur et Format
cat(stat_gen1_query(
  debut = "2020-01-01", fin = "2020-12-31",
  type_Rx = "DENOM", codes = c(47092, 47135, 48222),
  result_by = c("Teneur", "Format"),
  code_serv = c("1", "AD"), code_serv_filtre = "Exclusion",
  code_list = NULL, code_list_filtre = "Inclusion"
))

## Exclusion VS Inclusion des codes de service ou des codes de liste de mÃ©dicaments
cat(stat_gen1_query(
  debut = "2020-01-01", fin = "2020-12-31",
  type_Rx = "DENOM", codes = c(47092, 47135, 48222),
  result_by = c("Teneur", "Format"),
  code_serv = c("1", "AD"), code_serv_filtre = "Exclusion",
  code_list = c("40", "41"), code_list_filtre = "Inclusion"
))
}
