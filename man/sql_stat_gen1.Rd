% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SLQ_stat_gen1.R
\name{sql_stat_gen1}
\alias{sql_stat_gen1}
\title{Statistiques générales}
\usage{
sql_stat_gen1(
  conn = NULL,
  user = NULL,
  pwd = NULL,
  debut,
  fin,
  type_Rx = "DENOM",
  codes,
  result_by = NULL,
  code_serv = c("1", "AD"),
  code_serv_filtre = "Exclusion",
  code_list = NULL,
  code_list_filtre = "Inclusion"
)
}
\arguments{
\item{conn}{Variable contenant la connexion entre R et Teradata. Voir \code{\link{sql_connexion}}.}

\item{user}{Nom de l'identifiant pour la connexion SQL Teradata.}

\item{pwd}{Mot de passe associé à l'identifiant. Si \code{NULL}, le programme demande le mot passe. Cela permet de ne pas afficher le mot de passe dans un script.}

\item{debut}{Date de début de la période d'étude au format \code{"AAAA-MM-JJ"}.}

\item{fin}{Date de fin de la période d'étude au format \code{"AAAA-MM-JJ"}.}

\item{type_Rx}{\code{"DENOM"} ou \code{"DIN"}. Indique le type de code analysé.}

\item{codes}{Vecteur comprenant le ou les codes d'analyse au format numérique, sans zéros.}

\item{result_by}{Afficher les résultats par :
\itemize{
\item \strong{\code{"Période"}} : Résultats par période d'étude. Somme des montants de tous les \code{codes} analysés.
\item \strong{\code{"Teneur"}} : Résultats par teneur du médicament (\code{SMED_COD_TENR_MED}).
\item \strong{\code{"Format"}} : Résultats par format d'acquisition du médicament (\code{SMED_COD_FORMA_ACQ_MED}).
}}

\item{code_serv}{Vecteur de type \code{character} comprenant le ou les codes de service à exclure ou à inclure, sinon inscrire \code{NULL}.}

\item{code_serv_filtre}{\code{"Inclusion"} ou \code{"Exclusion"} des codes de service \code{code_serv}, sinon inscrire \code{NULL}.}

\item{code_list}{Vecteur de type \code{character} comprenant le ou les codes de catégories de liste de médicaments à exclure ou à inclure, sinon inscrire \code{NULL}.}

\item{code_list_filtre}{\code{"Inclusion"} ou \code{"Exclusion"} des codes de catégories de liste de médicaments \code{code_list}, sinon inscrire \code{NULL}.}
}
\value{
\code{data.table}
}
\description{
Tableau indiquant les statistiques générales d'un ou de plusieurs codes de médicaments selon certains critères.
}
\details{
Utiliser soit le paramètres \code{conn} ou la combinaison \code{user} et \code{pwd}.\cr\cr
\strong{debut, fin :} Doivent contenir le même nombre de valeurs.
}
\examples{
\dontrun{
library(inesss)
conn <- sql_connexion(uid = askpass::askpass("User"),
                      pwd = askpass::askpass("Mot de passe"))

# DENOM
dt1 <- sql_stat_gen1(
  conn = conn,
  debut = c("2017-01-01", "2018-01-01"),
  fin = c("2017-12-31", "2018-12-31"),
  type_Rx = "DENOM", codes = c(47092, 47135, 48222)
)

# DIN
dt2 <- sql_stat_gen1(
  conn = conn,
  debut = c("2017-01-01", "2018-01-01"),
  fin = c("2017-12-31", "2018-12-31"),
  type_Rx = "DIN", codes = c(30848, 585092)
)

# RÃ©sultats par PÃ©riode
dt3 <- sql_stat_gen1(
  conn = conn,
  debut = c("2017-01-01", "2018-01-01"),
  fin = c("2017-12-31", "2018-12-31"),
  type_Rx = "DENOM", codes = c(47092, 47135, 48222),
  result_by = "PÃ©riodes"
)
# RÃ©sultats par Teneur et Format
dt4 <- sql_stat_gen1(
  conn = conn,
  debut = c("2017-01-01", "2018-01-01"),
  fin = c("2017-12-31", "2018-12-31"),
  type_Rx = "DENOM", codes = c(47092, 47135, 48222),
  result_by = c("Teneur", "Format")
)

# Exclusion et Inclusion des codes de services et des codes de listes de mÃ©dicaments
dt5 <- sql_stat_gen1(
  conn = conn,
  debut = c("2017-01-01", "2018-01-01"),
  fin = c("2017-12-31", "2018-12-31"),
  type_Rx = "DENOM", codes = c(47092, 47135, 48222),
  code_serv = c("1", "AD"), code_serv_filtre = "Exclusion",
  code_list = c("40", "41"), code_list_filtre = "Inclusion"
)
}
}
